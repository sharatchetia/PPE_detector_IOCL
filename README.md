# PPE_detector_IOCL
A real-time PPE detection system using a custom YOLOv8 model and Django. It detects helmets and safety vests from live camera feed, identifies PPE violations with a time delay to reduce false alerts, logs violations, and allows downloading violation images and reports.
